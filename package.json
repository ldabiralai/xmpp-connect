{
  "name": "xmpp-connect",
  "version": "1.0.0",
  "description": "Check if xmpp service can be connected to",
  "main": "connect.js",
  "bin": "bin/connect",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-xmpp-client": "^3.0.2"
  }
}
